Для предпросмотра нужно открыть файл index.html в браузере.

HTML-код для вставки в разлел находится в файле html.txt (тот код, который как раз вставляется в блок .rnd-replace-max-height-350)
В HTML-коде (в файле index.html) присутствуют комментарии, которые помогут разместить контент.
!!! обратите внимание, что контент иногда дублируется для desktop и mobile версий, а именно блок с главным изображением

JS-код (ES6) для вставки в раздел находится в файле js.txt, если нужен такой же но минифицированный код, то он в файле js.min.txt

CSS-код для вставки в раздел находится в файле css.txt, если нужен такой же но минифицированный код, то он в файле css.min.txt

Данные файлы и папки находятся в папке for-site

Знаю что шрифт загружаете сами на сайт, поэтому необходимые файлы запросите у дизайнера. В данном проекте подключен только ttf и woff.
Я использовал https://4pda.to/static/font/OPPOSans-Regular.css

Подготовленные оптимизированные изображения находятся в папке images
